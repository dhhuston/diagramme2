/** Allowed font sizes (px) for schematic text blocks — keep in sync with properties select. */
export const TEXT_BLOCK_FONT_SIZES = [
  6.75, 9, 10, 11, 12, 13, 14, 16, 18, 20, 24, 28, 32, 40, 48,
] as const
