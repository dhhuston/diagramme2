[package]
name = "diagramme-dxf"
version = "0.1.0"
edition = "2021"

[dependencies]
diagramme-schema = { path = "../schema" }
diagramme-scene = { path = "../scene" }

[dev-dependencies]
serde_json = "1.0"
